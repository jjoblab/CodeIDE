# ADR 0040 : tas du daemon Gradle borné — les fixtures se construisent
# dans un environnement de test à mémoire contrainte (4 Go partagés avec
# le build lui-même) ; un daemon réutilisé par toutes les fixtures.
org.gradle.jvmargs=-Xmx512m
