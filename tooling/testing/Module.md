# tooling:testing

Fixtures du tooling Gradle : mini-projets réels (minimal, en erreur,
multi-module, tâche longue) et helpers de copie temporaire — consommés
par les tests d'intégration de `tooling:server` (§7.2).

Kotlin JVM pur, dépendance de test uniquement.

Statut : étape G1 (v0.26.0) livrée — voir le README du module et
`docs/TOOLING.md`.
