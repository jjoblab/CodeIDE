<?xml version="1.0" encoding="utf-8"?>
<!--
  Corps du dialogue de saisie d'un nom de fichier/dossier (étape 17) :
  création comme renommage — champ pré-rempli au renommage, validation
  partagée avec le wizard (1 à 64 caractères après trim, caractères
  interdits…), erreur localisée sous le champ.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/spacing_md"
    android:paddingTop="@dimen/spacing_md">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/champ_nom_fichier"
        style="@style/Widget.CodeIDE.TextField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:counterEnabled="true"
        app:counterMaxLength="64">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/saisie_nom_fichier"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:importantForAutofill="no"
            android:inputType="textNoSuggestions"
            android:maxLines="1" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Raison localisée d'un nom invalide (masquée si valide). -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/erreur_nom_fichier"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?attr/colorError"
        android:visibility="gone"
        tools:text="Le caractère « / » est interdit." />
</LinearLayout>
